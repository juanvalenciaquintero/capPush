import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.cappush',
  appName: 'capPush',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
